
echo "Quelle commande permet de renommer votre système ?"
echo
echo "sudo hostname flavinho"
echo

read -n1 -p "Would you like to execute the command? [y,n]" doit
echo
case $doit in
  y|Y) sudo hostname flavinho ;;
  n|N) echo "Ok, aborting..." ;;
  *) echo "Ok, aborting..." ;;
esac
