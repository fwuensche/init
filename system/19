echo "Listez les services qui se lancent automatiquement "
echo "lorsque la machine boot et indiquez le nom donné à ce type de service"
echo
echo "/etc/init.d/ | grep -v \".sh\""
echo
/etc/init.d/ | grep -v ".sh"
